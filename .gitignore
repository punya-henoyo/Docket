# Python
__pycache__/
*.pyc
.venv/
venv/

# uv
uv.lock.bak

# App runtime
docket_runs/
.env

# Docker build context extras
containers/**/*.log

# OS
.DS_Store

# demo app build artifacts
app/frontend/node_modules/
app/frontend/dist/

# console build
node_modules/
frontend/dist/
graphify-out/

# Microsoft Office lock files. Word writes ~$<name> beside any document you
# open; it carries the editing user's name and is not part of the repository.
# One reached a commit via `git add -A` after sbom/docket-sbom.docx was opened.
~$*
